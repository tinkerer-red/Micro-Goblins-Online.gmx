///is_hoarded()

//this function is used with enemy/summon objects to find out if another creature is hoarded with them
// it really only checks to see if another object of the same type has already set variables for what it's checking them copies those stats to prevent doing complex calls multiple times.

return place_meeting(x, y, object_index)   ///collision_rectangle( bbox_left, bbox_top, bbox_right, bbox_bottom, object_index, false, true);

