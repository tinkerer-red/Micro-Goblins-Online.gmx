// Basic debug info output. 

/*var mcx = "Mouse X: " + string(mouse_x);
var mcy = "Mouse Y: " + string(mouse_y);
var mcgx = "Grid X: " + string(floor(mouse_x / tileSize));
var mcgy = "Grid Y: " + string(floor(mouse_y / tileSize));
var gv = "Terrain Type: " + string(ds_grid_get(grid,floor(mouse_x / tileSize),floor(mouse_y / tileSize)));
var imgindexcheck =  "Tile Index: " + string(scr_at_bm_2dig(floor(mouse_x / tileSize),floor(mouse_y / tileSize),ds_grid_get(grid,floor(mouse_x / tileSize),floor(mouse_y / tileSize))));
var dsmapsize = "DS_Map size:" + string(ds_map_size(global.tile_map));

draw_set_colour(c_white);

draw_text(0, 0, "Min: ");
draw_text(100, 0, string(ds_grid_get_min(grid, 0, 0, width, height)));

draw_text(0, 20, "Max: ");
draw_text(100, 20, string(ds_grid_get_max(grid, 0, 0, width, height)));

draw_text(0, 40, "Fps: ");
draw_text(100, 40, string(fps_real));

draw_text(room_width - string_width("Press Space to restart generation, S to take screenshot"), room_height - string_height("Press Space to restart generation, S to take screenshot"), "Press Space to restart generation, S to take screenshot");

draw_text(0, 60, mcx);
draw_text(0, 80, mcy);

draw_text(0,100, mcgx);
draw_text(0,120, mcgy); 
draw_text(0,160, imgindexcheck);
draw_text(0,140, gv);

draw_text(0,180,dsmapsize);*/

